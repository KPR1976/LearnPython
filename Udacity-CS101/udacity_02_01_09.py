def find_second(search, target):
    search=danton
    f1 = search.find(target)
    f2 = search.find(target,f1+1)
    #d = a.find(b, c+1)
    #string.find(a, b, c + 1)
    return f2
#danton = "De l'audace, encore de l'audace, toujours de l'audace"
#print find_second(danton, 'audace')
